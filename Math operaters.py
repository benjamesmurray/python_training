#Math operators

#Floor division // = integer part of answer only
#Modulo % = Tells whether number is odd or even, or multiples? (when dividing by 2 then mod of 1 is odd, 0 is even)
#Power ** = multiply to the power of ** (square  root use (0.5)
#Negation - or + = convert sign of number between neg and pos (only converts once, cannot convert over and over)